name: "Build Kaos"
run-name: "Run by ${{ github.actor }}"
on: [push]
jobs:
  Test:
    runs-on: ubuntu-latest
    container:
      image: erlang:28
    steps:
      - name: Check out code
        uses: actions/checkout@v5
      - run: rebar3 test
